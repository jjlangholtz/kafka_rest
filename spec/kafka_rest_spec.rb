require 'spec_helper'

describe KafkaRest do
  it 'has a version number' do
    expect(KafkaRest::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
